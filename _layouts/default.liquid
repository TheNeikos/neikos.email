<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        {% if is_post %}
            <title>{{ title }}</title>
        {% else %}
            <title>Marcel Müller - Web Developer</title>
        {% endif %}
    </head>
    <body>
        <div>
            {% if is_post %}
                {% include '_layouts/post.liquid' %}
            {% else %}
                {{ content }}
            {% endif %}
        </div>
    </body>
</html>
